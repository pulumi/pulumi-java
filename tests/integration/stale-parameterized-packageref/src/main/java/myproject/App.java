package myproject;

// Placeholder — replaced during the integration test (after pulumi package add copies the
// random SDK sources) with the actual automation-API program that calls stack.preview() twice.
public class App {
    public static void main(String[] args) {
    }
}
