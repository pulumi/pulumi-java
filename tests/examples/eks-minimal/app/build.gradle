plugins {
    id 'application'
}

repositories {
    mavenLocal()
    maven { // The google mirror is less flaky than mavenCentral()
        url("https://maven-central.storage-download.googleapis.com/maven2/")
    }
    mavenCentral()
}

var pulumiJavaSdkVersion = System.getenv("PULUMI_JAVA_SDK_VERSION") ?: "0.0.1-SNAPSHOT"
var pulumiKubernetesSdkVersion = System.getenv("PULUMI_KUBERNETES_PROVIDER_SDK_VERSION") ?: "3.15.1";
var pulumiAwsSdkVersion = System.getenv("PULUMI_AWS_PROVIDER_SDK_VERSION") ?: "4.37.3";
var pulumiEksSdkVersion = System.getenv("PULUMI_EKS_PROVIDER_SDK_VERSION") ?: "0.37.1";

dependencies {
    implementation "io.pulumi:pulumi:$pulumiJavaSdkVersion"
    implementation "io.pulumi:kubernetes:$pulumiKubernetesSdkVersion"
    implementation "io.pulumi:aws:$pulumiAwsSdkVersion"
    implementation "io.pulumi:eks:$pulumiEksSdkVersion"
}

application {
    mainClass = project.hasProperty("mainClass")
            ? project.getProperty("mainClass")
            : 'io.pulumi.example.eksminimal.App'
}
