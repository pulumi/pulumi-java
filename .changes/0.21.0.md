## 0.21.0 - 2025-01-03

### Improvements

- [sdk] Update to Pulumi 3.144.1
- [runtime] Implement `GetRequiredPackages` for the Java language host
- [sdk] Support exporting plain stack output values with `Context.export`
- [sdk] Support `StackReference.getOutput` from generated programs
- [sdk] Implement `getOrganizationName` on `Deployment` and `Context`

### Bug Fixes

- [sdk] Emit the `Deployment` class when using Pulumi built-in functions in PCL `stack()` and `projectName()`

